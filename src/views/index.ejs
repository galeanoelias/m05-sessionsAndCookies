<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Clase 26</title>
</head>
<body>
    <form action="/store" method="post">
        <div>
            <label for="name">Ingrese su nombre</label>
            <input type="text" name="name" value="<%= locals.old && old.name %>">
            <% if (locals.errors && errors.name) { %>
                <small class="text-danger"><%= errors.name.msg %> </small>
            <% } %>
        </div>

        <div>
            <select name="color" >
                <option value="0" selected disable></option>
                <option type="radio" value="<%= locals.old ? old.last_name : "Red" %>">Rojo</option>
                <option type="radio" value="<%= locals.old ? old.last_name : "Blue" %>">Azul</option>
                <option type="radio" value="<%= locals.old ? old.last_name : "Green" %>">Verde</option>
                <option type="radio" value="<%= locals.old ? old.last_name : "Yellow" %>">Amarillo</option>
            </select>
            <% if (locals.errors && errors.color) { %>
                <small class="text-danger"><%= errors.color.msg %> </small>
            <% } %>

            <label for="">Recordarme</label>
            <input type="checkbox" name="remember" value="remember">
        </div>

        <div>
            <label for="email">Ingrese su email</label>
            <input type="email" name="email" value="<%= locals.old && old.email %>">
            <% if (locals.errors && errors.email) { %>
                <small class="text-danger"><%= errors.email.msg %> </small>
            <% } %>
        </div>

        <div>
            <label for="edad">Ingrese su edad</label>
            <input type="number" name="edad">
            <% if (locals.errors && errors.edad) { %>
                <small class="text-danger"><%= errors.edad.msg %> </small>
            <% } %>
        </div>

        <button type="submit">Enviar</button>
    </form>
</body>
</html>